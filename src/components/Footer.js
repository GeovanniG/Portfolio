import React from 'react';

const Footer = ({ className }) => {
    return (
        <div>
            <footer className={`footer ${className}`}>
                <p>Created by Geo</p>
            </footer>
        </div>
    )
}

export default Footer;