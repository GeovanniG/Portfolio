import React from 'react';

const Footer = ({ className }) => {
    return (
        <>
            <footer className={`footer ${className}`}>
                <p>Created by Geo</p>
            </footer>
        </>
    )
}

export default Footer;